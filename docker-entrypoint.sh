#!/bin/bash
set -e

# Компилируем переводы при каждом запуске контейнера
echo "🌐 Компиляция переводов..."
pybabel compile -d translations 2>&1 || echo "⚠️  Предупреждение: не удалось скомпилировать переводы (это нормально, если переводы уже скомпилированы)"

# Запускаем основную команду
exec "$@"

